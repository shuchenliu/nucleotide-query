const prefix = '/api';
export const ENDPOINTS = {
  SEQUENCE: prefix + '/sequence/',
  QUERY: prefix + '/query/',
  RECENT_SEARCH: prefix + '/search/recent',
  FREQUENT_SEARCH: prefix + '/search/frequent',
};
